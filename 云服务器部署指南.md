# 歌词工坊 - 云服务器部署指南

## 目录
- [前置准备](#前置准备)
- [部署方案](#部署方案)
- [快速部署](#快速部署)
- [详细步骤](#详细步骤)
- [域名配置](#域名配置)
- [SSL证书配置](#ssl证书配置)
- [常见问题](#常见问题)

---

## 前置准备

### 1. 云服务器要求
- **操作系统**: Ubuntu 20.04/22.04 LTS 推荐
- **CPU**: 1核及以上
- **内存**: 1GB 及以上
- **硬盘**: 10GB 及以上可用空间

### 2. 需要准备的软件
- SSH 客户端（用于连接服务器）
- SCP/SFTP 工具（用于上传文件）

---

## 部署方案

### 方案一：使用 Nginx（推荐）
适合生产环境，性能稳定，支持负载均衡。

### 方案二：使用 Docker
适合需要快速部署和隔离环境的场景。

### 方案三：使用 Node.js + PM2
适合需要后端服务的场景。

---

## 快速部署（方案一：Nginx）

### 在云服务器上执行以下命令

```bash
# 1. 登录服务器
ssh root@your-server-ip

# 2. 更新系统
apt update && apt upgrade -y

# 3. 安装 Nginx
apt install nginx -y

# 4. 启用 Nginx 服务
systemctl enable nginx
systemctl start nginx

# 5. 检查 Nginx 状态
systemctl status nginx
```

### 上传并部署网站

在本地计算机上执行：

```bash
# 将项目文件上传到服务器
scp -r /workspace/dist/* root@your-server-ip:/var/www/lyrics-studio/

# 上传 Nginx 配置
scp /workspace/nginx.conf root@your-server-ip:/etc/nginx/sites-available/lyrics-studio
```

在服务器上执行：

```bash
# 创建网站目录
mkdir -p /var/www/lyrics-studio

# 启用站点
ln -sf /etc/nginx/sites-available/lyrics-studio /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

---

## 详细步骤

### 步骤 1：准备部署文件

首先确保你的项目已经构建完成：

```bash
# 在本地项目目录执行
cd /workspace
npm run build
```

构建完成后，`dist` 目录将包含所有需要部署的文件。

### 步骤 2：上传文件到服务器

#### 方法 A：使用 SCP（推荐）

```bash
# 打包部署文件
cd /workspace
tar -czf lyrics-studio-deploy.tar.gz dist/ nginx.conf deploy.sh

# 上传到服务器
scp lyrics-studio-deploy.tar.gz root@your-server-ip:/root/
```

在服务器上解压：

```bash
cd /root
tar -xzf lyrics-studio-deploy.tar.gz
```

#### 方法 B：使用 Git（如果项目在 GitHub 上）

```bash
# 在服务器上克隆项目
git clone your-repo-url /var/www/lyrics-studio
cd /var/www/lyrics-studio

# 安装依赖并构建
npm install
npm run build
```

### 步骤 3：配置 Nginx

编辑 [nginx.conf](file:///workspace/nginx.conf)，修改 `server_name` 为你的域名：

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;  # 修改这里

    root /var/www/lyrics-studio;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # ... 其他配置保持不变
}
```

### 步骤 4：设置文件权限

```bash
# 设置正确的文件权限
chown -R www-data:www-data /var/www/lyrics-studio
chmod -R 755 /var/www/lyrics-studio
```

### 步骤 5：启用防火墙

```bash
# 允许 HTTP 和 HTTPS 流量
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

---

## 域名配置

### 1. 购买域名
在阿里云、腾讯云、Namecheap 等平台购买域名。

### 2. 配置 DNS 解析
在域名管理后台添加 A 记录：

| 记录类型 | 主机记录 | 记录值 | TTL |
|---------|---------|--------|-----|
| A | @ | 你的服务器IP | 600 |
| A | www | 你的服务器IP | 600 |

### 3. 验证 DNS 解析

```bash
# 在本地执行
ping your-domain.com

# 或者使用 nslookup
nslookup your-domain.com
```

---

## SSL 证书配置（HTTPS）

### 使用 Let's Encrypt 免费证书

```bash
# 1. 安装 Certbot
apt install certbot python3-certbot-nginx -y

# 2. 获取并安装证书
certbot --nginx -d your-domain.com -d www.your-domain.com

# 3. 按照提示完成配置
```

Certbot 会自动：
- 验证域名所有权
- 申请 SSL 证书
- 配置 Nginx 使用 HTTPS
- 设置自动续期

### 验证 HTTPS

访问 `https://your-domain.com` 确认证书生效。

---

## Docker 部署方案（方案二）

### 创建 Dockerfile

```dockerfile
FROM nginx:alpine

# 复制构建文件
COPY dist/ /usr/share/nginx/html/

# 复制 Nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

### 构建并运行

```bash
# 构建镜像
docker build -t lyrics-studio .

# 运行容器
docker run -d \
  --name lyrics-studio \
  -p 80:80 \
  --restart always \
  lyrics-studio
```

### 使用 Docker Compose

创建 `docker-compose.yml`:

```yaml
version: '3'
services:
  web:
    build: .
    ports:
      - "80:80"
    restart: always
```

启动：
```bash
docker-compose up -d
```

---

## 自动化部署脚本

### 使用提供的 deploy.sh

在服务器上执行：

```bash
# 赋予执行权限
chmod +x /root/deploy.sh

# 运行部署脚本
sudo bash /root/deploy.sh
```

### 自定义部署脚本

创建 `deploy-to-server.sh`:

```bash
#!/bin/bash
SERVER_IP="your-server-ip"
SERVER_USER="root"
LOCAL_DIR="/workspace/dist"
REMOTE_DIR="/var/www/lyrics-studio"

echo "正在上传文件..."
rsync -avz --delete $LOCAL_DIR/ $SERVER_USER@$SERVER_IP:$REMOTE_DIR/

echo "正在重启 Nginx..."
ssh $SERVER_USER@$SERVER_IP "systemctl reload nginx"

echo "部署完成！"
```

---

## 常见问题

### Q1: 访问网站显示 403 Forbidden
**A**: 检查文件权限：
```bash
chown -R www-data:www-data /var/www/lyrics-studio
chmod -R 755 /var/www/lyrics-studio
```

### Q2: 访问网站显示 404 Not Found
**A**: 确保 Nginx 配置中的 `try_files` 正确：
```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

### Q3: 如何更新网站？
**A**: 重新构建并上传：
```bash
# 本地
npm run build
rsync -avz --delete dist/ root@your-server-ip:/var/www/lyrics-studio/

# 服务器
systemctl reload nginx
```

### Q4: 如何备份网站？
**A**: 定期备份网站文件：
```bash
# 创建备份
tar -czf lyrics-studio-backup-$(date +%Y%m%d).tar.gz /var/www/lyrics-studio

# 恢复备份
tar -xzf lyrics-studio-backup-20240101.tar.gz -C /
```

---

## 性能优化建议

### 1. 启用 Gzip 压缩
已在 [nginx.conf](file:///workspace/nginx.conf#L17-L21) 中配置。

### 2. 配置 CDN
使用 Cloudflare 等 CDN 服务加速静态资源。

### 3. 启用浏览器缓存
已在配置中设置，静态资源缓存 1 年。

---

## 监控和维护

### 查看 Nginx 日志
```bash
# 访问日志
tail -f /var/log/nginx/access.log

# 错误日志
tail -f /var/log/nginx/error.log
```

### 检查服务器资源
```bash
# CPU 和内存使用
htop

# 磁盘空间
df -h

# 网络连接
netstat -tlnp
```

---

## 联系支持

如有问题，请查看项目文档或提交 Issue。
