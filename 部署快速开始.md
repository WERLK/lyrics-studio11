# 歌词工坊 - 部署快速开始

## 🚀 3 步完成部署

### 第 1 步：上传部署包到服务器

在本地计算机执行：

```bash
scp lyrics-studio-deploy.tar.gz root@你的服务器IP:/root/
```

### 第 2 步：登录服务器并解压

```bash
ssh root@你的服务器IP
cd /root
tar -xzf lyrics-studio-deploy.tar.gz
cd lyrics-studio-deploy
```

### 第 3 步：运行部署脚本

```bash
sudo bash server-deploy.sh
```

## ✅ 完成！

部署成功后，访问：
- 本地：http://localhost/
- 网络：http://你的服务器IP/

## 📋 下一步

### 1. 配置域名（可选）

在域名管理后台添加 A 记录：
- 主机记录：@
- 记录值：你的服务器IP

然后修改 Nginx 配置：
```bash
nano /etc/nginx/sites-available/lyrics-studio
```

将 `server_name _;` 改为 `server_name 你的域名.com www.你的域名.com;`

### 2. 配置 HTTPS（推荐）

```bash
apt install certbot python3-certbot-nginx -y
certbot --nginx -d 你的域名.com -d www.你的域名.com
```

### 3. 更新网站

当你修改了网站内容后：

```bash
# 1. 在本地重新构建
npm run build

# 2. 上传到服务器
scp -r dist/* root@你的服务器IP:/var/www/lyrics-studio/

# 3. 重启 Nginx（可选）
ssh root@你的服务器IP "systemctl reload nginx"
```

## 📖 详细文档

完整部署指南请查看：[云服务器部署指南.md](file:///workspace/云服务器部署指南.md)

## ❓ 常见问题

### Q: 部署后访问显示 403？
A: 检查文件权限：
```bash
chown -R www-data:www-data /var/www/lyrics-studio
chmod -R 755 /var/www/lyrics-studio
```

### Q: 如何查看 Nginx 日志？
A: 
```bash
# 访问日志
tail -f /var/log/nginx/access.log

# 错误日志
tail -f /var/log/nginx/error.log
```

### Q: 如何备份网站？
A:
```bash
# 创建备份
tar -czf lyrics-studio-backup-$(date +%Y%m%d).tar.gz /var/www/lyrics-studio
```

## 📦 部署包内容

```
lyrics-studio-deploy/
├── dist/                    # 网站构建文件
├── server-deploy.sh        # 自动部署脚本
├── nginx.conf              # Nginx 配置文件
└── 云服务器部署指南.md       # 详细文档
```

## 🎯 技术架构

- **Web 服务器**: Nginx
- **前端框架**: React + TypeScript + Vite
- **样式**: Tailwind CSS
- **部署目录**: /var/www/lyrics-studio

## 📞 技术支持

如有问题，请查看详细文档或提交 Issue。
